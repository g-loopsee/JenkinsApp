pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/g-loopsee/JenkinsApp.git'
            }
        }
        stage('Build') {
            steps {
                bat 'msbuild /t:Rebuild /p:Configuration=Release'
            }
        }

    }
}