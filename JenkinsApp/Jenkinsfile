pipeline {
    agent {
        docker {
            image 'mcr.microsoft.com/dotnet/sdk:7.0'
            args '-v //var/jenkins_home/workspace/DotnetPipeline:/var/jenkins_home/workspace/DotnetPipeline -v //var/jenkins_home/workspace/DotnetPipeline@tmp:/var/jenkins_home/workspace/DotnetPipeline@tmp'
        }
    }
    
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/g-loopsee/JenkinsApp.git'
            }
        }
        
        stage('Build') {
            steps {
                sh 'dotnet build --configuration Release'
            }
        }
        
        stage('Archive') {
            steps {
                archiveArtifacts artifacts: 'bin/Release/JenkinsApp.exe', fingerprint: true
            }
        }
    }
}
